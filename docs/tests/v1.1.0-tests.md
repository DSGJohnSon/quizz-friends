# Guide de Test - Version 1.1.0

## 🎯 Objectifs

Valider la robustesse de la connexion et la gestion du cycle de vie des sessions.

## 🧪 Scénarios de Test

### 1. Heartbeat & Statut Temps Réel

1. Ouvrir l'interface Host d'un côté et l'interface Player de l'autre.
2. Rejoindre la session en tant que joueur.
3. Vérifier que le badge est **vert (Online)** sur l'interface Host.
4. Couper l'onglet du joueur.
5. Attendre 7-10 secondes.
6. Vérifier que le badge passe au **rouge (Offline)** et qu'une alerte "⚠️ Nom s'est déconnecté" apparaît sur l'Host.

### 2. Reconnexion Réseau (Simulation)

1. Être dans la salle d'attente en tant que joueur.
2. Activer le mode avion ou couper le WiFi du device.
3. Vérifier que l'overlay **"Reconnexion..."** apparaît instantanément.
4. Réactiver la connexion.
5. Vérifier que l'overlay disparaît et que le joueur redevient actif.

### 3. Expiration de Session

1. Créer une session.
2. En base de données, modifier `expiresAt` pour une date passée (ex: hier).
3. Tenter de rejoindre via le code de session.
4. Vérifier que l'erreur **"Session has expired"** s'affiche.

### 4. Page de Résultats

1. Aller sur `/sessions/[ID_SESSION]/results`.
2. Vérifier que la liste des joueurs s'affiche correctement même si la session est expirée.

## 📋 Points de Vérification

- [ ] Heartbeat toutes les 5s (vérifier Network tab).
- [ ] Alerte Host disparaît après 15s ou clic sur X.
- [ ] Overlay joueur bloque toute interaction.
- [ ] Badge Offline clignote (animate-pulse).
