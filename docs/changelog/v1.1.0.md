# Changelog - Quiz Friends

## [1.1.0] - 2026-01-02

### ✨ Nouvelles Fonctionnalités

#### 📡 Robustesse & Reconnexion

- **Système de Heartbeat** : Les joueurs envoient un signal toutes les **3 secondes** pour confirmer leur présence.
- **Détection de Déconnexion Automatique** : Le serveur marque automatiquement un joueur comme déconnecté après **7 secondes** d'inactivité.
- **Overlay de Reconnexion** : Affichage d'un écran de blocage sur l'interface joueur basé sur le succès des requêtes HTTP (robuste même en 4G/WiFi mixte).
- **Alertes Host** : Notifications visuelles sur l'écran de l'animateur (toast de 15s) lorsqu'un joueur se déconnecte ou se reconnecte.

#### ⏳ Gestion de Session

- **Expiration de Session** : Les sessions expirent désormais automatiquement après 24h.
- **Blocage de Accès** : Impossible de rejoindre ou de rechercher une session expirée.
- **Page de Résultats** : Nouvelle route publique `/sessions/[sessionId]/results` pour consulter les résultats finaux d'une session, même après son expiration.

### 🔧 Technique

- Mise à jour du schéma Prisma avec les champs `expiresAt` (GameSession) et `lastActiveAt` (Player).
- Optimisation des routes API `getSession` avec paramètre `checkConnectivity` et polling Host toutes les **3 secondes**.
- Amélioration visuelle de la liste des joueurs avec badges de statut dynamiques.

### 🎨 UX/UI

- Ajout d'animations sur les alertes host.
- Nouveau design pour la page des résultats avec support futur des scores.
- Feedback visuel clair sur la connectivité des joueurs.

---

_Voir `v1.0.0.md` pour l'historique initial._

## ✨ Modifications mineures / Bug fixs

### [1.1.1] - 2026-01-02

- Correction des classes tailwind erronées
