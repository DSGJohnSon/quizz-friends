# Changelog - v1.2.0

**Date** : 2026-01-02  
**Titre** : Système de Score & Podium

## 🚀 Nouvelles Fonctionnalités

### 📊 Système de Score (Moteur)

- Ajout du modèle `ScoreEntry` dans Prisma pour l'historique des gains.
- Ajout d'un champ `totalScore` (cache) sur le modèle `Player`.
- Création du service `awardPoints` gérant l'atomicité et la synchronisation temps réel.
- Support des bonus (positifs) et malus (négatifs).

### 🕹️ Interface Host (Animateur)

- Affichage des scores en temps réel dans la liste des joueurs.
- **Contrôle manuel des scores** : Un champ de saisie et des boutons +/- permettent de modifier arbitrairement le score d'un joueur depuis la liste.
- Ajout du bouton **"Terminer la session"** quand le jeu est en cours (`IN_PROGRESS`).

### 📱 Interface Player (Mobile)

- Affichage de son propre score actuel sur le dashboard.
- Affichage des scores des autres participants dans la liste.

### 🏆 Résultats & Podium

- Refonte de la page `/results` avec un tri par score.
- Implémentation d'un design **Podium** premium pour le Top 3 (Or, Argent, Bronze).

## 📂 Organisation & Maintenance

- Restructuration du dossier `docs/` avec un nouveau dossier `features/`.
- Création de `docs/features/SESSION.md` et `docs/features/SCORE.md`.
- Mise à jour de `RECONNECTION.md` (déplacé dans `features/`).
- Amélioration du composant `Button` avec la variante `destructive`.

## 🧪 Tests

- Guide complet disponible dans `docs/tests/v1.2.0-tests.md`.
